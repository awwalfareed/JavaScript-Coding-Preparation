// Formula of a triangle         area = (base * height) / 2
const baseValue = 10;
const heightValue = 20;

// calculate the area
const areaValue = (baseValue * heightValue) / 2;

console.log(
  `The area of the triangle is ${areaValue}`
);